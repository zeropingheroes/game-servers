hostname = "Zero Ping Heroes";
password = "";
passwordAdmin = "";

description = "";

enableWhitelist = 0;
 
maxPlayers = 64;
 
verifySignatures = 2;
forceSameBuild = 1;

// Six alphanumeric characters
shardId = "def456";

disable3rdPerson = 0;
disableCrosshair = 0;

disablePersonalLight = 1;
lightingConfig = 0;

// In-game time when the server starts
// Set to "SystemTime" to use local time of this computer
// Set to "YYYY/MM/DD/HH/MM" for exact date and time
serverTime="2025/06/01/09/00";
serverTimeAcceleration = 24;
serverNightTimeAcceleration = 4;
serverTimePersistent = 0;

// Communication protocol to use
guaranteedUpdates=1;

// Number of players to allow to log into the server simultaneously, to prevent server slowdown as people connect
loginQueueConcurrentPlayers=5;

// The maximum number of players that can wait in login queue
loginQueueMaxPlayers=500;

// Server instance id, to identify the number of instances per box and their storage folders with persistence files
instanceId = 1;

// Check if the persistence files are corrupted, and if so, replace with empty files
storageAutoFix = 1;

class Missions
{
    class DayZ
    {
        // Mission to load on server startup. <MissionName>.<TerrainName>
        // Vanilla mission: dayzOffline.chernarusplus
        // DLC mission: dayzOffline.enoch
        template="dayzOffline.chernarusplus"; 
    };
};

steamQueryPort = 27016;

enableCfgGameplayFile=1;
